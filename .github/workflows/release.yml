name: Release

on:
  push:
    branches: [main]
  workflow_dispatch: {}

jobs:
  release:
    uses: jetscale-ai/booster/.github/workflows/release.yml@main
    with:
      image_name: thruster
      publish_to_dockerhub: true
    secrets: inherit
